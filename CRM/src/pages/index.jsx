const Index = () => {
    return (
      <>
        {/* <Routes>
  
          {AllRoutes.map((route) => (
            <React.Fragment key={route.id}>
              <Route
                path={route.path}
                index={route.index}
                element={route.element}
              />
            </React.Fragment>
          ))}
        </Routes> */}
      </>
    );
  };
  
  export default Index;